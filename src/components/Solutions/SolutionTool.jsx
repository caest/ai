import React from 'react';

const SolutionTool = ({ icon }) => {
  return (
    <div className="solution-tools-container">
      <img src={icon} alt="" />
    </div>
  );
};

export default SolutionTool;
